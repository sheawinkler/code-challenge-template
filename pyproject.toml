[project]
name = "code-challenge-template"
version = "0.1.0"
requires-python = ">=3.10"

[tool.black]
line-length = 100

[tool.ruff]
line-length = 100
lint.select = ["E", "F", "I", "B"]

[tool.ruff.lint.isort]
known-first-party = ["app"]
